# All renter-frontend requests go through the api-gateway (single entry point).
# Auth (/auth/*), GPU catalog and rentals (/api/v1/*) are all served by the gateway.
VITE_API_BASE_URL=http://localhost:8080
